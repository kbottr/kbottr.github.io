---
// Small rounded tag, e.g. "present".
---

<span class="pill mono"><slot /></span>

<style>
  .pill {
    display: inline-block;
    padding: 0 var(--space-8);
    border-radius: var(--radius-full);
    background: var(--accent-bg);
    color: var(--accent);
  }
</style>
